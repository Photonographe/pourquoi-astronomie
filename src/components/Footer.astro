---
---

<footer class="footer">
  <div class="footer-content">
    <div class="footer-section">
      <h3 class="footer-title">🌟 Continue ton voyage dans les étoiles !</h3>
      <p class="footer-subtitle">As-tu d'autres questions sur l'astronomie ? N'hésite pas à explorer toutes nos catégories !</p>
    </div>
    
    <div class="footer-grid">
      <div class="footer-column">
        <h4>Catégories</h4>
        <ul class="footer-links">
          <li><a href="/categories/terre-lune">🌍 Terre & Lune</a></li>
          <li><a href="/categories/soleil-etoiles">☀️ Soleil & Étoiles</a></li>
          <li><a href="/categories/planetes">🪐 Planètes</a></li>
        </ul>
      </div>
      
      <div class="footer-column">
        <h4>Explorer</h4>
        <ul class="footer-links">
          <li><a href="/categories/exploration-spatiale">🚀 Exploration spatiale</a></li>
          <li><a href="/categories/univers-galaxies">🌌 Univers & Galaxies</a></li>
          <li><a href="/categories/astronautes">👨‍🚀 Astronautes</a></li>
        </ul>
      </div>
      
      <div class="footer-column">
        <h4>Site</h4>
        <ul class="footer-links">
          <li><a href="/search">🔍 Recherche</a></li>
          <li><a href="/about">ℹ️ À propos</a></li>
          <li><a href="mailto:contact@pourquoi-astronomie.fr">✉️ Contact</a></li>
        </ul>
      </div>
      
      <div class="footer-column">
        <h4>Ressources</h4>
        <ul class="footer-links">
          <li><a href="https://www.nasa.gov/audience/forkids/" target="_blank" rel="noopener">NASA Kids</a></li>
          <li><a href="https://www.esa.int/kids/" target="_blank" rel="noopener">ESA Kids</a></li>
          <li><a href="https://spaceplace.nasa.gov/" target="_blank" rel="noopener">NASA Space Place</a></li>
        </ul>
      </div>
    </div>
    
    <div class="footer-bottom">
      <p>© 2025 Pourquoi ? L'astronomie - Fait avec ❤️ pour les petits explorateurs de l'espace</p>
      <p class="footer-sources">
        Sources scientifiques : NASA, ESA, CNES, Universités, télescope James Webb
      </p>
    </div>
  </div>
  
  <!-- Back to top button -->
  <button class="back-to-top" id="backToTop" aria-label="Retour en haut">
    ↑
  </button>
</footer>

<style>
  .footer {
    background: rgba(0, 0, 0, 0.5);
    margin-top: var(--space-xl);
    position: relative;
  }
  
  .footer-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: var(--space-lg) var(--space-md);
  }
  
  .footer-section {
    text-align: center;
    margin-bottom: var(--space-lg);
  }
  
  .footer-title {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: var(--color-primary);
  }
  
  .footer-subtitle {
    font-size: 1.1rem;
    color: var(--color-text-muted);
    max-width: 600px;
    margin: 0 auto;
  }
  
  .footer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
  }
  
  .footer-column h4 {
    color: var(--color-secondary);
    margin-bottom: 1rem;
    font-size: 1.1rem;
  }
  
  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .footer-links li {
    margin-bottom: 0.5rem;
  }
  
  .footer-links a {
    color: var(--color-text-muted);
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-block;
  }
  
  .footer-links a:hover {
    color: var(--color-primary);
    transform: translateX(5px);
  }
  
  .footer-links a:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
    border-radius: 4px;
  }
  
  .footer-bottom {
    text-align: center;
    padding-top: var(--space-md);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }
  
  .footer-bottom p {
    color: var(--color-text-muted);
    margin-bottom: 0.5rem;
  }
  
  .footer-sources {
    font-size: 0.9rem;
    font-style: italic;
  }
  
  .back-to-top {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    background: var(--color-primary);
    color: var(--color-dark);
    border: none;
    border-radius: 50%;
    width: 50px;
    height: 50px;
    font-size: 1.5rem;
    cursor: pointer;
    transition: all 0.3s ease;
    z-index: 1000;
    opacity: 0;
    visibility: hidden;
    transform: translateY(20px);
  }
  
  .back-to-top.visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }
  
  .back-to-top:hover {
    transform: translateY(-5px) scale(1.1);
    box-shadow: 0 10px 25px rgba(255, 215, 0, 0.3);
  }
  
  .back-to-top:focus-visible {
    outline: 3px solid var(--color-secondary);
    outline-offset: 2px;
  }
  
  @media (max-width: 768px) {
    .footer-content {
      padding: var(--space-md) var(--space-sm);
    }
    
    .footer-title {
      font-size: 1.5rem;
    }
    
    .footer-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-sm);
    }
    
    .back-to-top {
      bottom: 1rem;
      right: 1rem;
      width: 45px;
      height: 45px;
      font-size: 1.3rem;
    }
  }
</style>

<script>
  class FooterComponent {
    private backToTopBtn: HTMLElement;
    
    constructor() {
      this.backToTopBtn = document.getElementById('backToTop') as HTMLElement;
      this.setupEventListeners();
    }
    
    private setupEventListeners() {
      // Back to top button
      window.addEventListener('scroll', this.handleScroll.bind(this));
      this.backToTopBtn?.addEventListener('click', this.scrollToTop.bind(this));
      
      // External links tracking
      document.querySelectorAll('a[target="_blank"]').forEach(link => {
        link.addEventListener('click', this.trackExternalClick.bind(this));
      });
    }
    
    private handleScroll() {
      if (window.pageYOffset > 300) {
        this.backToTopBtn?.classList.add('visible');
      } else {
        this.backToTopBtn?.classList.remove('visible');
      }
    }
    
    private scrollToTop() {
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
      
      // Track analytics
      if (typeof gtag !== 'undefined') {
        gtag('event', 'scroll_to_top', {
          'event_category': 'navigation'
        });
      }
    }
    
    private trackExternalClick(e: Event) {
      const link = e.target as HTMLAnchorElement;
      const url = link.href;
      
      if (typeof gtag !== 'undefined') {
        gtag('event', 'click', {
          'event_category': 'external_link',
          'event_label': url,
          'transport_type': 'beacon'
        });
      }
    }
  }
  
  // Initialize when DOM is loaded
  document.addEventListener('DOMContentLoaded', () => {
    new FooterComponent();
  });
</script>
